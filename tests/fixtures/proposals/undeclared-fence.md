# A pair whose blocks declare no language

APRV-273: a fence with no info string is not a block this verb reads, so this
page proposes nothing at all and refuses rather than half-applying.

Current:

```
  network.call:              { autonomy: supervised }
```

Replace with:

```
  network.call:              { autonomy: manual }
```
