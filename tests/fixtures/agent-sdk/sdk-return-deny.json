{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "deny",
    "permissionDecisionReason": "hook-opaque: bash runs a shell script (segment: bash -c 'git push --force'). Rewrite it as a command the classifier can read, or run the effect through `approval run` with a granted token."
  }
}
