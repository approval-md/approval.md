{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "deny",
    "permissionDecisionReason": "hook-opaque: xargs runs a command built from its input (segment: xargs git push --force). Rewrite it as a command the classifier can read, or run the effect through `approval run` with a granted token."
  }
}
