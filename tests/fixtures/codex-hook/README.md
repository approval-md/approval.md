# Codex hook event fixtures

These are synthetic, native-shaped inputs for the APRV-310 probe sanitizer.
They contain no captured transcript, credential, account identifier, or real
model output. The live probe writes a separate sanitized JSONL record in its
scratch directory; promote a native observation here only after reviewing it.
