{"command":"backlog task edit APRV-151 --final-summary \"Root cause: not a classification bug and not worktree path anchoring. .claude/settings.json is tracked and byte-identical in HEAD, in commit 83d09c1 and in every worktree, and the hook classifies both incident files correctly inside worktrees (log seq 2534 gated .../worktrees/aprv-145-build/SPEC.md, seq 2781 gated .../worktrees/agent-a3f5d255372d43ac0/.github/workflows/ci.yml). The bypassing sessions produced NO hook records of any kind: between 2026-08-26T23:13Z and 2026-08-30T20:09Z the only session id in the log is the orchestrator's, and the 'gated sibling sessions' in the report are that orchestrator editing files inside sibling worktrees. Enforcement is entirely session-local, so a hook the harness never invokes is invisible to the runtime, and Claude Code treats a hook that cannot launch as non-blocking, which fails open.\n\nFix: the backstop outside the session. src/core/protected-grant.ts (pure) plus scripts/protected-grant-guard.mjs cross-check every protected-path change in a PR's diff against approval.granted policy.edit FILE-TOOL grants in the committed log, anchored on checkout roots derived from the log itself rather than suffix-matched, windowed at the branch point so a historical grant cannot authorize a future edit, with policy.protected_paths unioned across both sides of the diff. Every ambiguity (unreadable git state, unreadable log, unanchorable path, unparseable summary, missing policy, shell-only grant) resolves to UNAUTHORIZED.\n\nVerified: 19 new tests, every log built through the real append path and verified clean, plus three replays against the real primary log — the APRV-145 remediation resolves to its grant at seq 2536, the APRV-159 ci.yml remediation to seq 2787, and the same ci.yml change with the window moved past the grant is refused with exit 1. npm test 2461/2461 pass; oxlint clean. .github/workflows/ci.yml and .claude/settings.json were deliberately NOT edited; the workflow job to add is written out verbatim in the notes for human sign-off, and no settings change is proposed because the settings were already correct.\" 2>&1 | tail -2; backlog task edit APRV-151 --help 2>&1 | grep -i \"status\" | head -5","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/agent-a9de0a50bde9864f3"}