{"command":"gh pr create --base main --head claude/readme-restructure-review-eb9b23 --title \"APRV-412: split the README into a short entry and docs/README-extended.md\" --body-file - <<'EOF'\nTasks: APRV-412\n\n`README.md` becomes the short entry: pitch, quickstart, autonomy table, integration signposts, boundary statement. `docs/README-extended.md` carries the product tour and operating detail.\n\nBeyond the two drafts, this PR:\n\n- moves the three sections the drafts had dropped into the guide: How this compares, the per-key APPROVAL.md dictionary, and the seq 2 amend story\n- adds a fresh-directory doctor paragraph naming every `DOCTOR_FRESH_SKIPS` row, with no count\n- retargets `tests/docs-guard.test.ts` to the guide, drops the autonomy count phrase and doctor row-count literals, keeps every enum level and fresh-skip row asserted by name, and adds a test that the README links the guide\n- points the feature index, `llms-full.txt` and `llms.txt` at the guide's sections instead of README anchors that now land on a signpost\n- adds the guide to the npm `files` list\n- removes the pinned SHA and the Hermes clause from the version-scope paragraph\n\nVerified locally: build, docs-guard 17/17, lint, typecheck, a 53-reference anchor sweep with none unresolved, and `approval policy check` on the README example (both classes resolve `manual`, no load failure). `npm test` on this host has 41 pre-existing failures from a better-sqlite3 ABI mismatch under Node 26 and the SMTP mock's TLS handshake, none in touched files; CI is the verdict.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\nEOF\ngh pr merge claude/readme-restructure-review-eb9b23 --merge 2>&1 | tail -2","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/gated-messages-approval-60aa72"}