{"command":"cd /Users/carter/dev/approval-md/.claude/worktrees/aprv-383-hosted-daemon-identity && backlog task edit APRV-383 -s Done --append-notes \"Orchestrator review (Fable, 2026-09-20): accepted as built. Checked that the stamp and the allowlist refusal apply only inside a daemon process (isDaemonProcess), so CLI and hook appends are untouched; the field rides the chain hash and reaches no payload hash or token; a caller-supplied daemon field on EventInput is ignored. SPEC hunks stay proposed in design/hosted-daemon-identity.md section 7 for the human.\" 2>&1 | tail -1 && git add -A && git commit -q -m \"APRV-383: Done\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\" && git push -u origin claude/aprv-383-hosted-daemon-identity 2>&1 | tail -1 && gh pr create --base main --head claude/aprv-383-hosted-daemon-identity --title \"APRV-383: hosted daemon identity, the daemon field and the daemons allowlist\" --body-file - <<'EOF'\nTask: APRV-383. On the Agent Village path: APRV-399 (per-tenant Hermes skill) needs a hosted daemon whose records a tenant can attribute.\n\n## What this is\n\n- **Record field `daemon`**, optional and top level beside `actor`, so the chain hash covers it; it reaches no payload hash and no token. `EventInput` gained no member: a caller putting `daemon` on its input is ignored (pinned). Every record written before this validates unchanged; five new fixtures, `schema-validation` 2.7.0.\n- **Id derivation:** `APPROVAL_DAEMON_ID` from the launch environment wins; otherwise `daemon-` plus the eight hex digits `core/instance.ts` already derives from the instance path, the same id doctor prints as `keychain-scope`. Nothing stored; stable across restarts because the input is a path. A declared id failing the grammar is a refusal, never a fallback.\n- **Policy key `daemons`**, top level beside `senders`: absent means no restriction, empty admits none, read only from the attested policy. The restriction in force is the one from the most recent successful resolution, so an unreadable policy is not an escape from the list it carried a moment ago.\n- **Write boundary:** inside a daemon process only (`isDaemonProcess`), the append is stamped or refused with `daemon-id-invalid` or `daemon-not-allowed`, two new frozen append error codes (`refusal-unions` 21.0.0). CLI and hook appends are untouched.\n- Surfaces: the daemon `started` line, `approval status` (`daemon: {id, source, allowed}`), a new `daemon-identity` doctor row that skips rather than fails on an unlisted id (a tenant's policy names a daemon on someone else's machine).\n- `design/hosted-daemon-identity.md` states the hosting model and what is out of scope (process isolation, token scoping, billing), names this as attribution rather than authorization, and proposes the SPEC hunks (section 8 record field, two section 11.2 rows) without applying them.\n\nSPEC 11.1 invariants touched: 4 (a self-reported id never widens; listed gains nothing, unlisted is refused), 1 (allowlist read only from the attested policy), 6 (distinct machine-readable refusals).\n\n## Verification\n\nBuild, typecheck, lint exit 0. Targeted (daemon, daemon-identity, log, event-schema, fixtures, policy-load, conformance, conformance-regen, cli-status, cli-doctor): 546 of 546. `conformance/run.mjs`: 458 vectors pass, manifest ok. Full `npm test` on Node 26: 5001 of 5024, the 22 failures being this environment's known SMTP suite.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\nEOF\ngh pr merge 515 --merge 2>&1 | tail -1","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/open-source-licensing-658620"}