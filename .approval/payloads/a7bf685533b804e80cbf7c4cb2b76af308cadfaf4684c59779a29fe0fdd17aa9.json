{"command":"cd /Users/carter/dev/approval-md/.claude/worktrees/backlog-amend-ergonomics && backlog task edit APRV-343 -d \"Agents may not edit APPROVAL.md (policy.core), so policy changes travel as proposal documents under docs/proposals/ that quote each current line byte for byte with its replacement (docs/proposals/approval-md-2026-09.md is the shape: sections per task, Current and Replace-with fenced pairs, a later section may supersede an earlier one). On 2026-09-16 the human applied one by copying a prepared file over APPROVAL.md and running approval policy amend. Design and build a human-only verb, approval policy apply <proposal.md>, that parses the pairs, refuses before touching anything when any Current block does not occur exactly once in the live file (so a stale proposal cannot half-apply), applies the replacements in order with explicit supersession, shows the resulting diff, and on confirmation runs the amend (and its --pr once APRV-341 exists). The proposal format becomes a documented contract that agents write to and the verb reads; APRV-273's wrapper-fence hazard is handled by parsing only fenced blocks with a declared language. Open design questions to settle in the task: how supersession is declared, whether the verb also accepts a whole-file replacement, and how the values block (inert, SPEC 5.3) is treated identically to the policy block by the applier. Related: APRV-273, APRV-334, APRV-341.\" --plain 2>&1 | grep -c 'APRV-341'; git add backlog/tasks && git commit -q -m \"backlog: file APRV-341..343, policy amend ergonomics (amend --pr, attested-not-on-main row, policy apply)\n\nThree tasks from the 2026-09-16 policy amend: the amend verb finishing its own\ngit ceremony with --pr (APRV-341), doctor and up naming an attested policy that\nis not yet on main (APRV-342), and a human-only verb applying a proposal\ndocument's current/replacement pairs before the amend (APRV-343).\n\nCo-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>\" && git push -u origin backlog-amend-ergonomics 2>&1 | tail -1 && gh pr create --base main --title \"backlog: policy amend ergonomics tasks APRV-341, 342, 343\" --body \"Backlog-only PR (APRV-250). Three tasks filed from the 2026-09-16 policy amend, all To Do:\n\n- APRV-341: approval policy amend --pr commits the policy and its attestation, pushes, opens the pull request and arms the merge (the APRV-284 shape).\n- APRV-342: approval up and approval doctor name an attested policy commit that is not yet on main, with the command that lands it.\n- APRV-343: approval policy apply <proposal> applies a proposal document's current/replacement pairs and runs the amend in one human command.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\" 2>&1 | tail -1","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/emilia-protocol-comparison-af4ffb"}