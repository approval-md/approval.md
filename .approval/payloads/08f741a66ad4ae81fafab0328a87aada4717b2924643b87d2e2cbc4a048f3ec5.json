{"after":"Local Cursor Agent `Shell`, `Write`, and `Delete` calls are gated by `approval hook cursor` once `.cursor/hooks.json` is committed (`docs/cursor-hook.md`). Until that file is present in the checkout, classify uncertain shell commands with `approval hook classify -- \"<command>\"` and treat the result as binding. Prose compliance remains required where mechanical enforcement is absent.","before":"Cursor does not inherit the Claude Code PreToolUse hook automatically. Until an equivalent Cursor hook is configured, classify uncertain shell commands with `approval hook classify -- \"<command>\"` and treat the result as binding. Prose compliance remains required where mechanical enforcement is absent.","file":"/Users/carter/dev/approval-md/AGENTS.md","rule":"protected-path","tool":"Edit"}