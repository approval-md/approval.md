{"command":"cd /Users/carter/dev/approval-md/.claude/worktrees/aprv-396-release-notes && git push -u origin claude/aprv-396-publish-creates-release 2>&1 | tail -1 && gh pr create --draft --base main --head claude/aprv-396-publish-creates-release --title \"APRV-396: publish.yml creates the GitHub Release from the changelog section\" --body-file - <<'EOF'\nTask: APRV-396 (AC2 and AC3; AC1 was done by hand through the gate).\n\n**Draft on purpose, not armed.** `.github/workflows/` classifies `policy.edit.ci`, which is `autonomy: manual`, and nobody was awake to tap, so the two workflow hunks are not on this branch yet. Three tests in `tests/release-notes.test.ts` read those hunks and fail until they land; the branch is coherent once Carter applies them from `private/aprv-396/` (runbook there) and pushes.\n\n## On the branch\n\n- `scripts/release-notes.mjs`: extracts one version's changelog section; refuses with a distinct code per case (`no-section`, `undated-heading`, `duplicate-section`, `empty-section`, `bad-version`); `--check` lists dated sections. `## Unreleased` carries no version and can never match a tag.\n- `tests/release-notes.test.ts`: 27 tests, real-changelog extraction for 0.2.0 and 0.3.0, every refusal, CLI exit codes, and workflow assertions read through the repo's hardened YAML parser.\n- Docs: the changelog convention in `CHANGELOG.md`'s preamble, the Release job and the `contents: write` note in `docs/trusted-publishing-runbook.md`, the flow and refusal codes in `docs/dogfood-cutover.md`.\n\n## Waiting on a human\n\nThe `gitHead` bind (in the verify job right before `npm pack`, asserted in the tarball check) and the `release` job (`gh release create --verify-tag` or `edit`, tarball and sha256 attached). Verified fragments and an applier script are in the primary's `private/aprv-396/`. AC2's live half is proven by the next tag.\n\n## Verification\n\nBuild, typecheck, lint exit 0. `release-notes`: 24 of 27 (the 3 failing read the un-landed hunks). Full `npm test` on Node 26: 5006 of 5032, the 22-failure SMTP baseline plus those 3.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\nEOF","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/open-source-licensing-658620"}