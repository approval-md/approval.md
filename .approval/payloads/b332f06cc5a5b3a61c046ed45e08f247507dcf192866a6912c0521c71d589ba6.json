{"after":"and `hook claude-code` / `hook cursor`, which each read their event from the stdin this transport already owns. A conformance reader building the tool list from this section takes it to be the registry filtered by `human_only`, minus those three.","before":"and `hook claude-code`, which reads its event from the stdin this transport already owns. A conformance reader building the tool list from this section takes it to be the registry filtered by `human_only`, minus those two.","file":"/Users/carter/dev/approval-md/SPEC.md","rule":"protected-path","tool":"Edit"}