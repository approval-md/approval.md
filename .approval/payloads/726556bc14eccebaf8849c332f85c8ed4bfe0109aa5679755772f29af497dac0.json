{"command":"cat >> /Users/carter/.claude/projects/-Users-carter-dev-approval-md/memory/approval-md-session-practices.md <<'EOF'\n- **Policy ceremony checklist (2026-09-07, seq 27110):** `approval policy amend --commit` refuses unless EVERY declared class has a pin in src/core/policy-expectations.ts AND tests/dogfood.test.ts passes against the edited file (it pins approval_ttl's exact ms). Edit pins/tests in the primary, `npm run build`, then amend; the ceremony commits APPROVAL.md + log + pins together on `policy-amend-<seq>` and arms the PR, but NOT test files: push those onto that branch as a second commit (`git worktree add <dir> origin/policy-amend-<seq>` detached, commit, `git push origin HEAD:policy-amend-<seq>`). The diff's \"UNKNOWN KEY ... FAILS CLOSED\" lines for daemon.* keys are a false alarm (APRV-296). Do not edit APPROVAL.md until the previous amend PR merged. APRV-296 proposes trimming pins to safety classes.\n- **Sync after a ceremony or records merge:** the primary's APPROVAL.md equals main but differs from HEAD, so ff refuses; `git fetch -q origin main && git diff --quiet origin/main -- APPROVAL.md && git checkout -- APPROVAL.md && approval log sync` (proves identical first; the old bytes are in place for ~2s). QUEUE.md dirty from the daemon also refuses (APRV-292): stop the daemon, `git checkout -- .approval/QUEUE.md`, sync, `approval up`. Restart the daemon afterwards or every supervised-live class gates to a tap and Telegram is dark.\n- **Hook runs the dist of the checkout it is invoked in:** a lane worktree with a newer build gets the new hook behaviour (seen: APRV-287's 5m grace wording while #319 was unmerged). Multi-insertion SPEC edits = one policy.edit.spec question per Edit call; batch into one Edit where the anchors allow.\n- APRV-287 shipped (PR #319): expired waits withdraw after a 5m grace, stale requests collapse to one reject-all message on listener start, one card per multi-class command, and carried grants record `spent_by_task` so a completion clears the floor. Not closed: an Edit the hook allowed but the harness refused still counts as execution.failed (needs a harness-side fact; file when it bites).\nEOF\necho appended","cwd":"/Users/carter/dev/approval-md/.claude/worktrees/emilia-protocol-comparison-af4ffb"}