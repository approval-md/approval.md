---
id: APRV-74
title: approval setup identity | vault | sampling | telegram
status: Done
assignee:
  - '@fable'
created_date: '2026-08-18 01:39'
updated_date: '2026-08-18 02:56'
labels: []
milestone: m-10
dependencies:
  - APRV-73
priority: high
type: feature
ordinal: 73000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
doctor names what is wrong and cannot fix it; the Keychain commands are best practice you have to already know; discovering a Telegram chat id means curl against getUpdates and reading JSON by hand. Interactive setup verbs that store secrets in the keystore and write the .approval/env source map, refusing when stdin is not a TTY (printing the exact non-interactive commands). New cli/prompt.ts: readLine hoisted from amend.ts, readSecret (raw-mode byte loop, no echo, backspace, Ctrl-C aborts storing nothing, raw mode restored in finally), confirm. setup telegram: on macOS delegate token entry to security add-generic-password -w with inherited stdio (Apple no-echo prompt; the token never enters our argv; never -w <value>), else readSecret; getMe verification (reuse doctor probe + redact), print @username; ask the human to message the bot; getUpdates with NO OFFSET EVER (read without acknowledging so a running listener keeps its callbacks; comment why); dedupe candidates newest-first; confirm; store token via source map, chat id inline. setup identity exempt from the human-only gate (it declares the identity; say so); vault and sampling gated like vault set; generate randomBytes(32) base64, never print. Nothing under setup appends, attests, or edits APPROVAL.md.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 cli/prompt.ts with readLine/readSecret/confirm; amend.ts uses it; every setup subcommand refuses non-TTY or --json at exit 2 printing the non-interactive commands
- [x] #2 setup identity, vault, sampling write the source map and store secrets in the keystore without printing them; re-run asks before replacing
- [x] #3 setup telegram: token acquisition, getMe, human-sends-message, getUpdates with no offset, candidate confirmation, storage; token never in any argv/output/error (redaction wraps every message); assert no getUpdates body carried an offset
- [x] #4 Log byte-compared across a full run; interactive paths tested via injected prompt and keystore (real Keychain never touched); telegram-mock gains messageUpdate
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Opus subagent, worktree from aprv-73 branch. 2. cli/prompt.ts (readLine hoisted from amend, readSecret raw-mode, confirm); cli/setup.ts with identity/vault/sampling/telegram; non-TTY refusal; keystore writer per source map; telegram getUpdates no-offset discovery. 3. Injected prompt + keystore in tests; telegram-mock messageUpdate; log byte-compare. PR.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Opus subagent build, PR #42. cli/prompt.ts: readLine hoisted from amend (private copy deleted), readSecret raw-mode byte loop (UTF-8-aware backspace, Ctrl-C aborts storing nothing exit 2, raw mode restored in finally), confirm; Prompter interface is the injection seam AND the TTY gate (createPrompter returns null when !isTTY). Every subcommand refuses non-TTY or --json at exit 2 printing the exact non-interactive commands. Keystore per platform: macOS keychain (prompted values via security own no-echo prompt with stdio inherit so the token never enters our process; generated values via stdin then argv fallback for freshly-minted-never-used secrets only, disclosed when taken); Linux secret-service (values on stdin); neither -> plaintext literal behind a typed yes with the plaintext warning. Services approval-tg-token / approval-vault-passphrase / approval-sampling-secret. FABLE FIX ON REVIEW: a scratch-keychain probe (create-keychain, piped add-generic-password -w, find, delete) showed security exiting 0 while storing nothing findable under the service; every keychain store now verifies by read-back and success means the keystore returns exactly the generated bytes. setup telegram: getMe redacted, human messages the bot, getUpdates with NO offset ever (mock now honours allowed_updates so a queued callback_query is proven to survive; a test walks every request body for an offset key), candidates newest-first, confirm, optional sendMessage proof default no, storage. identity exempt from the human-only gate (it declares the identity); vault/sampling gated; setup vault warns default-no before re-keying an existing vault (no re-key verb exists: reviewer-weigh); setup sampling stores under APPROVAL_SAMPLING_SECRET and prints the audit block for policy amend when the policy names nothing (edits no policy). Write failures reuse env-file-io (no new frozen code). Log byte-identical across every run. SPEC 10.1 line drafted (flagged). +34 tests, 1464.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Interactive setup verbs: no-echo prompts, OS keystore storage with read-back verification, .approval/env source-map writes, Telegram chat discovery via offset-free getUpdates, non-TTY refusal with documented alternatives. PR #42.
<!-- SECTION:FINAL_SUMMARY:END -->
