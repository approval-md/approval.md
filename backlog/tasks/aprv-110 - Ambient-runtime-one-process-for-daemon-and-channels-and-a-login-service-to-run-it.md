---
id: APRV-110
title: >-
  Ambient runtime: one process for daemon and channels, and a login service to
  run it
status: Done
assignee:
  - '@fable-wave1'
created_date: '2026-08-20 08:55'
updated_date: '2026-08-29 20:09'
labels:
  - ux
  - daemon
  - channels
milestone: m-12
dependencies: []
priority: medium
ordinal: 102000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Human question 2026-08-20: "will users always need to create a listener and daemon runner?" At v0.1 yes: approval daemon run and approval channel telegram listen are two foreground processes, and dispatch deliberately lives in the listener because it holds the channel credential and the approver identity (SPEC 10.3, amended APRV-55). That placement is explicitly an implementation choice the SPEC permits moving: "a later build MAY move dispatch into the daemon with no change to any event, projection, or channel interface." This task makes the gate ambient in two steps. (1) approval up (or daemon run --with-channels): one supervised foreground process running the daemon loop plus every channel the policy configures (telegram long-poll listener, web queue page), with per-part crash isolation (a channel that dies is restarted with backoff and reported as a DaemonEvent; the daemon loop never dies with it), clean SIGINT/SIGTERM shutdown of all parts, and one --json event stream interleaving the existing DaemonEvent and listener output (additive union only). The token print sites and decision recording keep their exact current behaviour and identity handling; the human identity and channel credentials come from the environment the operator launched the process with (invariant 7, no implicit env loading). (2) approval setup service: writes and loads a launchd plist (macOS) or a systemd user unit (Linux) that runs approval up in the primary checkout at login, environment sourced the way the operator chooses explicitly (the unit file names the variables or an EnvironmentFile the human authors; setup never copies secret VALUES into the unit, only names, keystore references stay in the keystore via approval env evaluated by a wrapper the human reads). setup service is HUMAN-ONLY and interactive like the other setup verbs; it prints the unit for review before writing; uninstall verb or flag included. Logs of the service go to a file under the operator choice, never into .approval/. Fail closed: up refuses to start a channel whose credential is missing rather than starting half-armed silently (it reports and continues with the parts that can run, matching doctor vocabulary). SPEC touch: 10.2/10.3 one-paragraph amendment (dispatch MAY run inside the daemon process; the reference runtime now does), flagged for sign-off; the withdrawn/edit-message flow (APRV-106) and dispatch dedup must keep their listener-restart semantics (a restart re-sends pending, a duplicate never a silence).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 approval up runs the daemon pass and the configured channels in one foreground process; SIGINT/SIGTERM stops all parts cleanly; a crashing channel restarts with backoff and is reported, and the daemon loop survives it
- [x] #2 Decision recording, token printing, withdrawn message edits and re-send-on-restart semantics are byte-compatible with the separate processes (tests reuse the existing channel and daemon suites against up)
- [x] #3 approval setup service writes a launchd plist or systemd user unit for review before installing, names variables and never copies secret values, and can uninstall; human-only, interactive by refusal
- [x] #4 SPEC 10.2/10.3 amendment drafted and flagged; docs (README harness section, dogfood runbook, mcp-demo prerequisites) updated to one process; npm test and lint clean
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. New src/cli/up.ts: the supervisor. Owns the flag table (daemon run flags + --as/--payloads/--payload-dir/--api-base/--poll-timeout/--port/--no-telegram/--no-web/--restart-backoff), one signal wiring, one sink. Parts: daemon (Daemon from daemon/daemon.ts, unchanged), telegram (the listener loop), web (startWebChannel, already exported for this). Per-part crash isolation: a part that throws is reported and restarted after a doubling backoff; the daemon loop settles the verb.
2. Refactor, do not duplicate, in src/cli/channel-telegram.ts: split setUp into prepareListen (returns a discriminated refusal instead of printing) and the verb shell that maps refusals to usage/IO codes, and split runListener into listenLoop (no process signal handlers, fresh DispatchState per attempt, so a restart re-sends) and the verb shell that installs handlers and chooses an exit code. Token print sites, handlerFor, dispatchPending, annotate flow: untouched.
3. Fail closed per part: a channel whose credential or identity is missing is not started. It is reported as part_unavailable in doctor vocabulary (check/status skip/detail/fix, the same sentences doctor prints) and up continues with the parts that can run.
4. Event stream: additive union only. Every DaemonEvent and every listener line passes through verbatim; the supervisor adds up_started, part_started, part_unavailable, part_failed, part_restarted, up_stopped. No field is added to an existing shape, so the decision object and the token panel stay byte-identical.
5. approval daemon run --with-channels delegates to the same implementation before parsing, so there is one code path and two spellings.
6. approval setup service (src/cli/setup-service.ts): human-only, interactive by refusal through setup-common front/requireHuman, prints the launchd plist or systemd user unit for review BEFORE writing, names variables or an operator-authored EnvironmentFile and never a value, --uninstall removes it, logs go to an operator-chosen path and never into .approval/.
7. Wiring: main dispatch case up, verb registry entries for up and setup service, help constants UP_HELP and SETUP_SERVICE_HELP with why: anchors, docs/cli-reference.md sections for those anchors, ROOT_HELP usage lines, layering exception list.
8. tests/up.test.ts composes the existing helpers (tests/scenario.ts, tests/telegram-mock.ts) rather than copying either suite: one process delivering, deciding, printing the token, annotating a withdrawal, expiring by the daemon and annotating that, restarting a failing channel while the daemon keeps ticking, refusing a channel with no credential, and stopping cleanly on SIGTERM and SIGINT.
9. Docs: README harness section, docs/dogfood-cutover.md, examples/mcp-demo.md prerequisites, docs/cli-reference.md. SPEC 10.2/10.3 amendment as ONE batched Edit, LAST.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DESIGN SIGN-OFF 2026-08-25 (Carter, in session): approved as designed. approval up (daemon + channels in one supervised process, per-part crash isolation with backoff, clean signal shutdown, byte-compatible decision/token/restart semantics) and approval setup service (launchd/systemd user unit at login, human-only, prints the unit for review, names variables never values, uninstallable). Fail-closed on missing channel credentials: report and run the parts that can. SPEC 10.2/10.3 drafting (AC 4) may proceed. Consumes APRV-125's verbs for automatic log sync once both exist.

CONTINUATION LANDED 2026-08-29 (1 of 4). WORKTREE DIVERGENCE, READ FIRST: this session was harness-isolated to worktree wf_49a904c5-d90-4, and every git command aimed at the prior lane worktree wf_57158fdc-034-5 was refused by the isolation guard, so branch aprv-110-ambient-runtime could not be committed to. The prior agent files were ported forward instead, and the single commit sits on branch worktree-wf_49a904c5-d90-4 at 2ac7703, based on main e31ffff which is a descendant of the lane base 13b86e2. The commits in between touched only events.jsonl, APPROVAL.md and tests/dogfood.test.ts, none of which this task edits, so the port is clean and 2ac7703 cherry-picks onto the lane branch without conflict.

CONTINUATION (2 of 4) WHAT THIS SESSION ADDED on top of the prior agent rough-in of up.ts, setup-service.ts and the channel-telegram/daemon/setup/help/main edits. (a) main.ts was calling commandUp without importing it, and up.ts read an optional health detail as a required string; both fixed. (b) Verb registry entries for up and setup service, both human_only with notes, plus their pins in the human-only list in tests/cli-instructions.test.ts. up inherits the refusals of daemon run and channel telegram listen together and cannot be weaker than either; setup service installs a standing capability, which is the one thing an agent must never arrange for itself. (c) layering.test.ts DAEMON_IMPORTERS_ALLOWED widened to include up.ts, with the reason recorded in the list comment: it is the second spelling of the same verb, and routing the loop through a third module would have hidden that identity rather than removed the dependency. (d) docs/cli-reference.md gained the up and setup-service sections the why anchors point at. (e) Four help texts went back under the 25-line cap: UP_HELP and SETUP_SERVICE_HELP were new, and DAEMON_RUN_HELP and SETUP_HELP were pushed over by the prior rough-in. Two phrases are pinned by tests and had to survive the trim on a single line each, REFUSES WHEN STDIN IS NOT A TERMINAL in SETUP_HELP and backgrounding is the operator in DAEMON_RUN_HELP. (f) README harness section, docs/dogfood-cutover.md and examples/mcp-demo.md now lead with the one-process story and keep the separate pair as the still-supported alternative. (g) An unused parameter was dropped from disarmCommand for lint.

CONTINUATION (3 of 4) AC2, tests/up.test.ts, 14 cases, all green. It composes rather than restates: the daemon suite fixture shape (a real case directory, policy attested and request made through the CLI own verbs) with the telegram suite mock Bot API. Cases: one process delivers, decides, prints the token and writes the queue; the decision line compared against a real run of channel telegram listen (key set compared whole, semantic values compared by name, token panel compared with the tokens blanked); the token reaches stdout and never the chat; an unset credential and a missing identity each skip the channel in doctor four fields while the daemon runs; a mistyped poll timeout or restart backoff is refused outright; the web part skips when no port is declared; a channel that falls over restarts with backoff and re-sends while the daemon keeps ticking; the daemon expires a lapsed request and the channel annotates it in one process; SIGINT and SIGTERM each stop every part with a verifiable log; daemon run --with-channels reaches the same function; help and an unexpected argument. THREE REAL BUGS THE SUITE FOUND, worth knowing. First, a fixture whose declaration carries a payload_hash with no stored bytes is skipped payload-unavailable and never presented, so the fixture now binds a real payload and passes --payloads, exactly as the telegram suite does. Second, the action key must be unique per case: one mock serves the whole file, so a constant key let one case delivered prompt satisfy the next case delivery check and hand it a callback nonce from a process that had already exited. Third, and the one that matters: a run that configures telegram without pointing at the mock long-polls the real Bot API and hangs the suite inside spawnSync. There is now an assertOffline guard on every spawn in the file that refuses a token-carrying run with no local --api-base, which is the subprocess equivalent of the assertLocal promise the telegram suite makes on every channel it constructs.

CONTINUATION (4 of 4) INVARIANTS AND VERIFICATION. SPEC 11.1 invariant 7 is the one this change touches: no verb loads the environment source map implicitly. up reads the bot token, the chat id and the approver identity from process.env only, through the same resolvers the separate listener uses, and setup service writes a unit that names those variables and never carries a value (either a wrapper the human reads that evaluates approval env, or an EnvironmentFile the human authored and this verb never opens). The suite scrubs those three variables from every child environment so a developer own shell cannot make an unconfigured case pass. Also touched, and held: enforcement paths read only verified records (every restart re-derives the pending queue from the verified log rather than from a remembered set), refusals are machine-readable and distinct (the new ListenRefusalCode union, and part_unavailable carrying doctor own check/status/detail/fix rather than a second vocabulary), and no gate-typed event accepts a caller timestamp (no new append path was introduced at all: decisions still go through recordChannelDecision to the human-only decide). VERIFICATION: npm test 2318 pass 0 fail; npm run lint clean; npm run conformance 221/221 vectors, 101 controls, manifest ok. STILL OPEN: AC4 SPEC amendment. The 10.2/10.3 paragraphs were NOT edited in this session, because a SPEC edit is manual-class and blocks on a human tap. The exact intended text is recorded in the session report under spec_edit for replay after the tap; nothing else in the task is waiting on it.

CONTINUATION (addendum) AC3 COVERAGE. setup service also got its own cases in tests/cli-setup.test.ts, kept deliberately OUT of that suite shared SUBCOMMANDS walk: the two shared refusal tests assert on the scripted keystore path every other subcommand prints, and this verb writes no credential and no environment line. What is asserted instead: the non-terminal stdin refusal at exit 2 writing nothing and printing its own scripted path; the --json refusal; the help stance; the launchd plist naming the three variables while assigning none of them, with both StandardOutPath and StandardErrorPath outside .approval; XML escaping of a working directory containing an ampersand; the systemd unit carrying an EnvironmentFile only when the operator named one; the wrapper evaluating approval env by default and sourcing the operator file otherwise; unit paths, log homes, arm and disarm commands for both platforms; execWords for a node entry, an installed bin and an override; the full print-then-confirm flow where answering no prints the whole unit and writes nothing, answering yes writes exactly the bytes that were shown and still does not start it, and --uninstall prints the stop command, removes the file, and says nothing to remove the second time; a --logs path inside .approval refused; a non-human --as refused. Final counts after this addendum: npm test 2330 pass 0 fail, lint clean, conformance 221/221. Commit amended to 4815434 on branch worktree-wf_49a904c5-d90-4.

Merged: PR #147 as main 37a0e1c through the merge queue. AC evidence: AC1 tests/up.test.ts (825 lines) drives one supervised process running the daemon pass plus configured channels, pins per-part crash isolation (a killed channel restarts with backoff and surfaces as a DaemonEvent while ticks continue), and pins clean SIGINT/SIGTERM shutdown of all parts; AC2 the byte-compatibility bar is met by composing the existing daemon and telegram-channel suite machinery against up (decision recording, token print sites, withdrawn message edits, re-send-on-restart), with the credential variables scrubbed from every child environment so an unconfigured case cannot pass from a developer shell; AC3 tests/cli-setup.test.ts (+270 lines) pins setup service as human-only and interactive by refusal, printing the unit for review before writing, naming environment variables and never carrying a value, and uninstalling; AC4 the SPEC 10.3 amendment (dispatch MAY run inside the daemon process; the reference runtime now does; per-part supervision mandated) landed flagged pending sign-off via the granted policy.edit replay, and README, docs/dogfood-cutover.md, examples/mcp-demo.md, and docs/cli-reference.md carry the one-process story. Verification: lane run 2318 pass, conformance 221/221, lint clean; serial full-suite re-run on the commit clean (fail 0, exit 0). 11.1 invariants: 7 touched and held (credentials from the launch environment only); 1 held (restarts re-derive pending from the verified log); 6 held (ListenRefusalCode union; part_unavailable speaks doctor vocabulary). The SPEC edit rode a separate commit (b64ad90) because the lane hit the asleep-human tap timeout; the recorded text was replayed byte-identically against the standing grant.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
The gate goes ambient: approval up runs the daemon loop and every configured channel in one supervised foreground process (per-part crash isolation, clean signal shutdown, additive --json stream, fail-closed on missing credentials), and approval setup service installs a reviewed launchd/systemd user unit that names variables and never values. Byte-compatible with the separate processes by composed-suite evidence. Merged as PR #147 (main 37a0e1c); verified 2318 tests + serial fail-0 re-run, conformance 221/221, lint clean.
<!-- SECTION:FINAL_SUMMARY:END -->
