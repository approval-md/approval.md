---
id: APRV-109
title: >-
  Attest and amend from the phone: the policy ceremony collects its human act
  through a channel
status: Done
assignee:
  - '@fable-wave1'
created_date: '2026-08-19 22:13'
updated_date: '2026-08-29 21:28'
labels:
  - ux
  - channels
  - policy
  - spec
milestone: m-12
dependencies: []
priority: medium
ordinal: 101000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Human feedback 2026-08-19: "this all feels quite manual - why do i have to run these git commands myself? why do i have to edit the spec and merge it?" The gate already reduces DECISIONS to a tap, but the two attestation ceremonies (policy attest, policy amend) still require the human at a terminal, because attestation is human-only and identity is config-declared (SPEC 11): an agent running amend would be claiming the human identity. The log-advance commit was retired as a human chore the same day (an agent staging .approval/ is a protected-path manual action, so it is one tap); this task retires the last terminal ceremony. DESIGN: (1) A new gate-typed request kind for attestation: an agent (or the amend verb run by an agent identity) prepares the policy edit on a branch and appends an attestation REQUEST carrying the policy file SHA-256 and the semantic diff summary as COMPUTED fields; channels render it like a manual action prompt: the hash, the class-resolution changes in before -> after form, the load advisory verdict, and Approve/Reject buttons. (2) A tap by the approver appends policy.updated (the attestation) under the human identity the listener holds, exactly as a grant lands today; the attested hash is the one the prompt displayed, so the human signed bytes they were shown (the phone shows the diff and the hash; the full policy text is reachable as the payload). (3) The amend git ceremony (two-file commit, PR) then proceeds agent-side; the attestation seq is cited in the commit as today. (4) Fail closed: no channel configured, or the tap times out, means the ceremony refuses and nothing is attested; the withdrawn flow (APRV-106) retires stale attestation prompts. (5) SPEC amendments: 10.1/10.3 (attest via channel), 11 (the human act is the tap; the trust boundary statement unchanged: the listener holds the human identity the way it does for grants). CAUTION to resolve in design: a diff on a phone is small; the prompt MUST carry the semantic diff and the load advisory, never just a hash, and SHOULD refuse to render a diff too large for the channel rather than truncating it silently (fall back to "review at a terminal"). Related decision recorded the same day: agents MAY perform the log-advance commit and the APPROVAL.md edit mechanics behind the gate (protected-path manual approvals); CLAUDE.md dogfooding wording update drafted for the human in the notes here.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 An attestation request renders on Telegram, web and cli with the policy hash, the semantic diff and the load advisory as computed fields; Approve appends the attestation under the approver identity; Reject and timeout attest nothing
- [x] #2 approval policy amend run by an agent identity prepares everything, requests attestation through the channel, and completes the two-file ceremony only on the tap; run by a human identity it behaves exactly as today
- [x] #3 A diff too large for the channel refuses to the terminal path rather than truncating; fail closed on no channel; withdrawn retires stale prompts
- [x] #4 SPEC 10.1/10.3/11 amendments drafted and flagged; npm test and lint clean
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Schema: add gate-typed `policy.proposed` and `policy.declined` to the event enum, with per-type rules (proposer is human:/agent:, decliner is human:, computed payload fields required) and valid + invalid fixtures.
2. Core `src/core/policy-proposal.ts`: `proposeAttestation()` reads the live policy bytes once, computes sha256, the semantic diff (baseline accepted only when its own hash equals the latest attestation) and the load advisory, refuses `diff-too-large` rather than truncating, stores the full policy text in the payload store, and appends `policy.proposed` with a runtime-assigned ts and compare-and-append. `decideAttestation()` is human-only: it re-hashes the live file, refuses `proposal-stale` on any drift, and appends `policy.updated` (attest) or `policy.declined` (decline) citing the proposal seq.
3. Gate: four new codes on `GATE_REFUSAL_CODES` (`diff-too-large`, `proposal-not-found`, `proposal-stale`, `policy-already-attested`); regen the conformance refusal-unions vector.
4. Channels: the prompt is an ordinary `ChannelRequest` so telegram, web and cli render it through their existing paths. Two additive COMPUTED members (`policy_diff`, `policy_load`) carry the semantic diff and the advisory; `buildPendingQueue` emits open proposals alongside pending approvals; `assembleBatch` never folds one into a batch; `recordChannelDecision` routes an attestation action key to `decideAttestation` so a tap lands as `policy.updated` under the listener-held human identity.
5. CLI: `approval policy amend --as agent:<id>` prepares the edit, proposes through the channel, waits for the tap, and completes the two-file ceremony citing the attestation seq. Under a human identity the verb is byte-compatible with today.
6. Tests: core propose/decide, schema fixtures, shared render shape across the three channels, amend agent path, oversized-diff refusal, fail-closed on no channel and on timeout.
7. SPEC 10.1/10.3/11 amendments flagged "(Amended APRV-109, pending sign-off.)" in ONE batched edit, last.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CLAUDE.md wording drafted for the human (the file is yours): in "Dogfooding", replace "The committed log has one writer: the daemon" paragraph sentence "log-touching commits never ride feature branches" context with an added sentence: "Log-advance commits and APPROVAL.md edit mechanics MAY be performed by an agent: staging .approval/ or the policy file is a protected-path manual action, so the human act is the approval tap; attestation itself remains human-only (via APRV-109 once built, at a terminal until then)." And in the Permissions summary, "Require approval first" already covers it via policy.edit; no change needed there.

DESIGN SIGN-OFF 2026-08-25 (Carter, in session): approved as designed. Attestation requests render through channels with policy hash + semantic diff + load advisory as computed fields; the approve tap appends the attestation under the listener-held human identity exactly as grants land; oversized diffs refuse to the terminal path rather than truncate; fail closed on no channel / timeout / reject. The CLAUDE.md wording change drafted in these notes remains Carter's to commit by hand. SPEC 10.1/10.3/11 drafting (AC 4) may proceed.

IMPLEMENTATION 1 of 7 (branch/worktree divergence, read this first). This continuation was launched against worktree wf_57158fdc-034-6 on branch aprv-109-attest-from-phone, but the harness isolated the session to a different worktree, wf_49a904c5-d90-5, and REFUSED every git operation targeting the other path. The prior agent's work there was uncommitted and unreachable to git from here. The lane branch is also checked out in that other worktree, so it could not be checked out here either. Resolution: the prior agent's twelve files were copied in verbatim by path (they were last written at 00:50 and the session was dead by 07:39, so nothing was raced), the remainder was finished here, and the single commit landed on this worktree's own branch worktree-wf_49a904c5-d90-5. It is a clean cherry-pick onto aprv-109-attest-from-phone: the prior base 13b86e2 is an ancestor of this base e31ffff, and the only files that differ between the two bases are events.jsonl, APPROVAL.md and tests/dogfood.test.ts, none of which this change touches.

IMPLEMENTATION 2 of 7 (what the prior agent left, kept unchanged). src/core/policy-proposal.ts (new, the whole ceremony core), the event schema additions with five fixtures, the four new gate refusal codes, the EventType additions in core/log.ts, and the channel edits in contract.ts, tagging.ts, telegram.ts and web.ts. The naming choice policy.proposed / policy.declined was re-evaluated against the SPEC section 8 enum-versioning bullet and kept: additions to the closed set are exactly what that bullet governs, the pair is the seventh and eighth after payload.pruned, approval.withdrawn, the execution.indeterminate pair and the reconciliation pair, and nothing in the existing enum or in the runtime collides with either name. The header comment in policy-proposal.ts states the reason proposed was chosen over requested: approval.requested is the approval lifecycle with its own TTL, budgets and grant, and reusing the word would invite a projection to treat an attestation as an ordinary authorization.

IMPLEMENTATION 3 of 7 (the amend verb, AC 2). src/cli/amend.ts now accepts --as agent:ID. Everything up to the attestation is byte-identical to the human path: the same policy resolution, the same baseline recovery, the same diff, the same load advisory, the same --require-load refusal. At the point the human path calls appendAttestation, the agent path calls collectAttestation instead, which fails closed on no channel, appends the proposal, and polls the verified log until the proposal reaches a terminal state. Only an attested proposal continues; the seq the git ceremony cites is the policy.updated seq, so every downstream commit message, runbook and JSON field is unchanged. New flags --wait (default 15m), --interval (default 2s) and --note are refused under a human identity rather than ignored, because an operator who passed --wait believes they asked for the channel ceremony. The terminal y/N confirmation is skipped on the agent path: the confirmation IS the tap, and asking this process for one would be asking the party under oversight to confirm its own amendment. recoverBaseline gained a bytes field so the verified HEAD blob it already recovered is handed to the core rather than recovered twice.

IMPLEMENTATION 4 of 7 (fail closed, AC 3). Four ceremony-level refusal codes on the amend verb, each of which attests nothing and commits nothing. no-channel when the live policy declares no channels, checked BEFORE the proposal is appended so a question nobody could be asked is never recorded; a policy that does not load is the same answer for a stronger reason, since the channel table lives in the policy and unknown resolves to the stricter path. propose-failed carries the core code, which is diff-too-large for a diff past the 60-line / 2400-character budget the smallest supported channel can show whole. attestation-declined for a tap on Reject. attestation-timeout for a lapsed --wait and for a superseded prompt. All four exit 1 except an append failure, which exits 4: a gate refusal is 1, and the command was well-formed. Stale prompts retire themselves rather than needing a withdrawal event: a proposal carries the proposer's wait_until, proposalState derives expired from it, openProposals drops it, so it leaves every channel queue by derivation. cli/channel-telegram.ts was extended so a delivered attestation prompt is annotated and disarmed when it settles, which requestState could never do for it because an attestation key has no approval.requested behind it. The four proposal states map onto headlines the channel already has: attested reads granted, declined reads rejected, superseded reads withdrawn, lapsed reads expired.

IMPLEMENTATION 5 of 7 (invariants touched, and one correctness fix). Invariant 2, gate-typed events never accept caller timestamps: both new events are gate-typed and take ts from tick(options) at the write boundary; no public function in policy-proposal.ts has a ts parameter, and the tests drive the clock by injection. Invariant 4, self-reported fields never reduce scrutiny: the three fields an approver decides on are COMPUTED from the bytes, and ProposeInput has no field for a hash, a diff or an advisory, so the refusal of a caller-authored value is structural rather than validated. The one piece of caller material, the diff baseline, is re-hashed and discarded unless it equals the latest attestation, and the proposal falls to hash-only mode with the reason recorded. Invariants 1 and 5 are inherited unchanged: every read is readVerifiedRecords and every append carries the compare-and-append precondition. Invariant 6: four codes added to GATE_REFUSAL_CODES and one to CHANNEL_TAG_REFUSAL_CODES, all pinned. CORRECTNESS FIX made here, worth review: recordAttestationDecision now resolves the policy FILE from the listener's own gateOptions.policy rather than from the proposal record. A proposal records the policy basename by design, since a log is meant to be copied and an absolute path would name the proposer's home directory, so resolving from the record would have made the re-hash depend on the listener's working directory. It failed closed either way, and this makes it the right path.

IMPLEMENTATION 6 of 7 (tests, conformance, verification). New tests/policy-proposal.test.ts covers propose and decide end to end through the real append path: computed hash, diff and advisory; an unverifiable baseline falling to hash-only mode; a diff too large refused with the log bytes unchanged; already-attested; a system actor refused; a human proposer allowed; the tap attesting the displayed hash under the approver identity; an agent refused actor-not-human; bytes that moved refusing proposal-stale; a decline attesting nothing; a second answer refused already-decided; a lapsed prompt appending nothing and leaving openProposals; supersession. tests/channels-contract.test.ts gained five cases proving the prompt renders as an ordinary request with policy_diff and policy_load computed and the summary claimed, that the queue carries prompts after the approvals whose rules they change, that a tap appends policy.updated and mints no token, that a reject appends policy.declined, and that a moved-bytes prompt is skipped. tests/event-schema.test.ts gained four cases for the two new types plus the optional proposed_seq on policy.updated. tests/cli-amend.test.ts gained five for the agent path. Conformance regenerated: refusal-unions to 2.0.0 because a pinned expectation moved, schema-validation to 1.1.0 because vectors were added. gate-verdicts.v1.json is byte-unchanged.

IMPLEMENTATION 7 of 7 (SPEC edit NOT landed: the tap did not come). AC 4's SPEC amendment was attempted twice as one batched Edit and the policy.edit hook timed out both times at 540s, so the human was asleep. Everything else is committed; the SPEC is byte-unchanged. The intended edit is a single insertion into SPEC.md immediately BEFORE the line reading three-hash 10.4 Adapters and hard enforcement, and the exact text to insert is five paragraphs, reproduced verbatim below between the markers.

SPEC-EDIT-BEGIN

**Attestation collected through a channel.** Deciding a request is a tap. Deciding which rules are in force was still a terminal session, because attestation is human-only (§11) and identity is config-declared, so an agent running the amendment ceremony would be claiming the human identity. A channel closes that gap without moving the boundary. A party that has prepared a policy edit appends `policy.proposed`, a channel renders it as an ordinary `manual` prompt, and the approver's tap appends the `policy.updated` attestation under the human identity the listener holds, exactly as a grant lands today. The following four paragraphs amend §8, §10.1, this section, and §11 respectively. (Amended APRV-109, pending sign-off.)

*Amends §8.* The v0.1 event set gains `policy.proposed` and `policy.declined`, the seventh and eighth additions to the draft set of sixteen; readers of a v0.1 log may encounter them and a verifier written against the draft enum MUST be updated to accept them. Both are gate-typed, so both take their `ts` from the runtime at the write boundary. `policy.proposed` carries a `human:` or `agent:` actor, since either may prepare an amendment, and MUST NOT carry `system:`, because the runtime proposes no policy of its own. Its payload names the policy file, the SHA-256 of its exact bytes, the semantic diff summary, and the load advisory. `policy.declined` carries a `human:` actor for the reason `approval.rejected` does, names the proposed hash, and names the `policy.proposed` sequence number it answers. The acceptance is `policy.updated`, which already existed and remains the only event an attestation is; when it answers a prompt it MAY carry that same `proposed_seq`. That a cited sequence number is an open proposal in this log is a rule the runtime enforces, since a schema sees one record.

*Amends §10.1.* The CLI block gains no verb. `approval policy amend` accepts an `agent:` identity, under which it prepares the edit and the diff as it does today, appends the proposal, waits for the answer, and completes the git ceremony only once the attestation has landed, citing that sequence number in the commit as it always has. Under a human identity the verb is unchanged. Every ending short of a tap attests nothing and commits nothing: no configured channel, a decline, a lapsed deadline, a superseded prompt, and a diff too large to render each refuse with their own machine-readable code, and the policy edit stays in the working tree.

*Amends §10.3.* An attestation prompt is an ordinary channel request and inherits every rule of this section, including the computed and claimed split and the verbatim payload presentation of §10.4, where the payload is the policy text itself. Two computed fields are added for it: the semantic diff of what the proposed bytes change about class resolution, in before and after form, and the advisory saying whether those bytes load. The hash, the diff, and the advisory MUST all be derived by the runtime from the bytes; a proposing caller supplies none of them, so a proposal cannot show an approver one story and attest a different file. A prompt carrying only a hash asks a human to sign for sixty-four characters and is a conformance failure. A diff a channel cannot show whole MUST be refused rather than truncated, naming the terminal path as the repair: a prompt showing two thirds of a policy change collects a signature for the third it did not show. A prompt whose policy bytes have changed since it was rendered MUST leave the queue rather than be shown, and the answer to such a prompt MUST be refused, so the hash the approver was shown is the hash that gets attested or nothing is.

*Amends §11.* The trust boundary of this section is unchanged. Human identity is config-declared, the boundary is the local machine, and attestation proves that someone with local control signed off rather than who. What this amendment changes is where that person's act is collected: the listener holds the human identity for an attestation the way it already holds it for a grant, and the same reasoning that makes a tapped grant a human decision makes a tapped attestation one. An agent MUST NOT append `policy.updated` or `policy.declined`, in the schema and in the runtime both.

SPEC-EDIT-END

VERIFICATION on the committed tree: npm test 2338 pass, 0 fail; npm run lint clean; npm run conformance 228 of 228 vectors, 0 failed, 104 negative controls. One flake was seen once and did not recur: a telegram mock socket in the APRV-135 memory test.

Merged: PR #146 as main 8453a0f through the merge queue, after one reconciliation merge (6107096) resolving the refusal-unions regen conflict with the merged APRV-146 (suite bumped to 3.0.0 recording both union changes; the regen also picked up schema-validation vectors for this branch five new event fixtures). AC evidence: AC1 channels-contract and policy-proposal tests pin the attestation prompt rendering on telegram, web, and cli with the policy hash, semantic diff, and load advisory as COMPUTED fields, Approve appending policy.updated under the listener-held human identity with proposed_seq, and Reject/timeout attesting nothing; AC2 cli-amend tests pin approval policy amend under an agent identity preparing everything, appending policy.proposed, completing the two-file ceremony only on the tap and citing the attestation seq, and byte-compatible behavior under a human identity; AC3 the too-large-diff refusal to the terminal path, the fail-closed no-channel refusal, and the APRV-106 withdrawn flow retiring stale attestation prompts are pinned (four new ceremony refusal codes joined the gate union, plus proposal-stale on the channel tag union); AC4 SPEC 10.1/10.3/8/11 amendments landed flagged pending sign-off via the granted policy.edit replay (5bbc38a), and lane verification was 2338/2338 with conformance 228/228; the merged tree verified 2376/2376 with conformance clean at 104 controls and lint clean. 11.1 invariants touched: 2 (the new gate-typed events take ts at the write boundary; a caller timestamp on the reporting side is ignored) and 4 (hash, diff, and advisory are computed by the runtime from the bytes, never caller-claimed).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
The policy ceremony attests from the phone: policy.proposed renders through channels as an ordinary manual prompt carrying computed hash, semantic diff, and load advisory; the tap appends policy.updated under the listener-held human identity; amend under an agent identity completes the two-file ceremony only on that tap. Fail closed on no channel, decline, timeout, staleness, and oversized diffs. Merged as PR #146 (main 8453a0f); verified 2338 lane tests, 2376/2376 on the merged tree, conformance clean, lint clean.
<!-- SECTION:FINAL_SUMMARY:END -->
