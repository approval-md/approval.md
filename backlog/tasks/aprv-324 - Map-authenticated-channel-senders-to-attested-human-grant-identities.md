---
id: APRV-324
title: Map authenticated channel senders to attested human grant identities
status: In Progress
assignee:
  - '@opus-lane-identity'
created_date: '2026-09-08 22:53'
updated_date: '2026-09-17 08:19'
labels: []
dependencies:
  - APRV-249
references:
  - 'https://github.com/approval-md/approval.md/issues/137'
priority: medium
type: feature
ordinal: 241000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
GitHub issue #137 requests grant attribution to the actual Telegram callback sender rather than the shared listener process identity. Design an operator-attested sender-to-human mapping inside the existing machine trust boundary, coordinated with APRV249, without treating caller-supplied identity as proof.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A reviewed design defines sender mapping, unknown-sender refusal, per-class approvers, audit fields and safe migration from listener identity.
- [x] #2 Implementation tests cover two distinct senders, missing or stale mapping, spoofed request fields, unauthorized sender and concurrent decisions before changing production attribution.
- [x] #3 Issue #137 remains open until implemented and verified; documentation states exactly what the channel can authenticate.
- [x] #4 SPEC 10.3 is corrected as part of this implementation: it says the Telegram callback is verified against approver identity, while src/channels/telegram.ts verifies only the configured chat id and never reads the sender (SPEC 5.2 and 11 are already accurate); the sentence states exactly what the channel authenticates before this task and after it, and the amendment is called out to the human
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Implement design/channel-sender-identity.md, whose section 5 is the change list and whose section 7 is the test list. 2. New pure module src/core/sender-identity.ts: the sender vocabulary (telegram only, the one transport with a sender fact worth mapping per design section 2), the person-to-sender index inverted at lookup, and the four resolution modes of design section 3.2. 3. schema/policy.schema.json gains approvers.id.senders, additive and optional, keys from the closed channel vocabulary. 4. src/core/policy-load.ts parses it, and a sender mapped to two approvers is a LOAD failure with the new code sender-ambiguous, so the policy does not load and every class resolves manual. 5. src/channels/contract.ts resolves the sender before decide: no sender or no mapping configured for that channel means the configured actor exactly as today; a mapped sender becomes the actor; an unmapped sender is refused with a new closed union CHANNEL_DECISION_REFUSAL_CODES and writes one audit.decision_refused carrying the observed id and no grant. 6. src/core/gate.ts records payload.sender, payload.sender_source and the top-level channel field on decision records; event schema constrains all three at the write boundary. 7. src/channels/telegram.ts reads callback_query.from.id after the existing chat check and passes it as part of the reported gesture; nothing in the message body is read. 8. approval doctor gains a sender-mapping row per design item 13. 9. Nineteen tests from design section 7 in tests/sender-identity.test.ts, against the mock Bot API and scratch logs built through the real append path. 10. SPEC 10.3 sentence corrected per criterion 4, plus 5.2, 11.1 and 11.2 entries the design requires; README dictionary row; docs.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Delivered by the closeouts lane, 2026-09-16: design/channel-sender-identity.md, a reviewed design and no runtime change. Classifies policy.edit.design (supervised-retro).

AC1 checked. Section 1 traces what is recorded today with line references: resolveHumanActor at attest.ts:432-440 is the whole of identity, each channel calls it once at start-up (channel-telegram.ts:436, channel-web.ts:370-371, channel.ts:278-279) and then passes one fixed actor to recordChannelDecision for every decision that surface collects, and the record is appended with that literal string at gate.ts:2910-2921. The Telegram callback is checked against the configured chat id at telegram.ts:3327-3331 and the update's from object is never read anywhere in that file. Section 2 then states, per transport, what can actually be proved: exactly one field in the whole system is worth mapping, Telegram's from.id, and even that is evidence about an account rather than about a person.

Section 3 defines the mapping: an additive optional senders key inside the existing approvers roster in APPROVAL.md, so it inherits the human-only attestation ceremony that already protects the roster and an agent cannot add itself as an approver's sender. It is written person to sender, per channel, and resolved at the channel boundary before decide is called, so the gate's authorization logic including namesApprover and actor-not-approver is completely unchanged. Section 3.3 makes an unmapped sender a refusal recorded as an audit.decision_refused with a new sender-unmapped code carrying the observed id, explicitly NOT a fallback to the configured actor, because that is today's behaviour dressed up as a feature and would still let a stranger in the chat approve as Carter. A sender mapped to two approvers is a load-time refusal; the runtime does not resolve an operator's ambiguity by picking.

Section 4 specifies the audit fields: set the channel field the base schema already defines and the grant never sets; add payload.sender and payload.sender_source, both optional and additive, so the ABSENCE of sender is itself meaningful and says the attribution came from configuration. It also says what must never be recorded: from.username, because it is mutable and reusable and a log carrying it grows a field that reads like identity and decays into a lie, and anything the sender claims about themselves, which is the self-reported-fields invariant.

Section 6 is the safe migration: phase 1 records the sender without enforcing, so the operator learns from their own log which ids have been deciding and therefore what to write in the policy; phase 2 maps and warns; phase 3 refuses, gated on a clean doctor row so nobody arrives there by accident. Rollback is removing the key at the next attestation, with no code change and no log repair, which is the argument for the mapping living in policy rather than a separate store. A deployment that never adopts it is byte-identical to today, and that is the first test in section 7.

One finding worth Carter's attention independently of this design. SPEC.md section 10.3 at line 460 describes Telegram as callback verified against approver identity. That is not what the code does: the callback is verified against a configured chat id and no approver identity is read from it. SPEC 11 line 549 and SPEC 5.2 line 137 are both precise and correct, so this is a local imprecision rather than a contradiction of the model, but it is exactly the sentence a reader would rely on. It should be corrected whether or not the rest of this design is ever built, and it is item 1 of the required changes. No SPEC edit was made by this lane.

AC2 NOT checked, and this is a judgement call worth stating rather than burying. The criterion reads: implementation tests cover two distinct senders, missing or stale mapping, spoofed request fields, unauthorized sender and concurrent decisions BEFORE changing production attribution. Read as written, it is a gate on the implementation phase and not on a design: with no resolution code in the tree there is nothing for those tests to exercise, and tests written against a stub would be evidence about the stub. Writing them anyway and checking the criterion would be checking an acceptance criterion on code presence rather than on verified behaviour, which the finalization guide forbids and which this repository exists to make expensive.

So the five cases are enumerated in full instead, as section 7 of the design, as the implementation task's specification: nineteen numbered tests grouped as compatibility (the byte-identical regression for a policy with no senders), two distinct senders (different actors and matching payload.sender on separate action keys, the second sender refused already-decided on one key, a mapped sender the class does not name refused actor-not-approver), missing or stale mapping (an unmapped id refused with the observed id recorded, a mapping removed between request and tap refused rather than honoured under the old one, an ambiguous mapping refused at load so everything resolves manual), spoofed request fields (a body claiming a different id ignored in favour of from.id, a username matching another approver's id changing nothing, a web post carrying a sender field ignored entirely because that channel can authenticate nobody, and a foreign chat still rejected by the chat check that runs first, because the mapping widens who may decide and never where from), unauthorized sender, and concurrent decisions (two senders racing one action key producing exactly one grant and one token with the loser getting a re-derived already-decided, two senders on two keys both landing, and a resolution racing a re-attestation either using the policy it verified or refusing policy-drift). Every one is specified to be built through the real append path.

AC2 closes when the implementation task runs them green. AC3 checked: the task references issue #137, the issue stays open because the design is not the behaviour it asks for, and the design states exactly what each channel can authenticate, as a table in section 2 with the recommendation that the table belongs in SPEC section 10.3 rather than only in design/. Adding a comment to the issue itself is a network write and a gated action, so it is left to the human.

Task stays In Progress on AC2.

Delivered in pull request #414 (lane/closeouts).

Folded in 2026-09-17 on Carter's word: Lane 5 found that SPEC 10.3 overclaims (callback verified against approver identity; the code checks the chat id only, src/channels/telegram.ts around 3327). The correction rides this task's implementation rather than a separate edit, because this task is what makes the sentence true.

Implementation, 2026-09-17 (identity lane, branch lane/sender-identity-324). design/channel-sender-identity.md is the specification and every part of its section 5 change list is in the diff; its section 7 test list is tests/sender-identity.test.ts, nineteen tests in the design's own order and grouping, plus one unit-coverage test. 20 tests, 20 pass, 0 fail, exit code 0.

What the runtime does now. src/core/sender-identity.ts is the whole of the decision-time logic: pure, total, no I/O, four resolution modes. src/channels/telegram.ts reads callback_query.from.id in routeCallback, after the configured chat check and before any decision is assembled, and reports it on the gesture; nothing in the message, the callback data or the username is read. src/channels/contract.ts resolves that observation against the attested policy BEFORE calling decide, so the gate's authorization logic is untouched and namesApprover still refuses actor-not-approver with no new path. src/core/gate.ts records payload.sender, payload.sender_source and the top-level channel field on the decision record; the event schema constrains all three at the write boundary.

Decision where the design was impractical as written. Section 3.2 says the sender rides on ChannelActorOptions. It rides on ChannelDecision instead: onDecision registers ONE handler per listener process and the actor options are built from fixed setup data, so a per-listener field cannot carry a per-gesture fact, and giving each channel a mutable slot to stash it in would be the channel-held state section 10.3 forbids.

Decision where the design was silent. Mode 1 is keyed on the POLICY rather than on the transport: a supplied sender with no senders block for that channel is today's configured attribution, because a channel that starts supplying senders would otherwise lock every existing installation out of its own gate at the first upgrade. Writing the first entry for a channel is what turns enforcement on for it. A policy that does NOT load is not a policy with no mapping and fails closed: a supplied sender is refused sender-unmapped rather than attributed. Nothing is stranded by that, because the CLI channel supplies no sender, so a terminal can always decide and always repair the file, which is where a policy.core edit has to happen anyway.

Decision on section 7 tests 1 and 18, which contradict each other. Test 1 asks for a log byte-identical to today under a policy with no senders; test 18 asks that every decision record carry channel, which section 4 item 1 states outright and calls worth doing on its own merits. The channel field wins and test 1 is implemented as the attribution claim it is: same actor, same decision, same payload keys, neither new key present, on all three channels. The one additive difference is the channel field, asserted explicitly in test 1 so the difference is on the record rather than discovered later.

Two things the design listed that this deliberately did not do. Section 3.5 channel binding stays a separate task, as the design recommends. The Telegram note-reply path records an audit review rather than an approval decision, so it keeps the listener identity; extending sender resolution to reviews is a larger question about who a review is attributed to and it is not this task's criterion.

Invariants touched, and how each is preserved. Self-reported fields never reduce scrutiny: the only identity input in the system is the transport's own attribution, callback_query.from.id; from.username is never a key and is never recorded, a body or payload claiming a user id is ignored, the web channel supplies no sender at all and the policy schema refuses a senders entry for it, and test 8, test 9, test 10 and test 19 pin all four. Gate-typed events never accept caller timestamps: unchanged, no new parameter carries one, and ts still comes from the injected clock at the write boundary. Human-only classes stay inert to agents, no verb mints authority for them: a human-only class refuses class-human-only and the surface records no sender for it, so the runtime performs no identity resolution about a class it does not transact in (test 13). Validate at the write boundary: payload.sender, payload.sender_source and the relaxed actor rule on audit.decision_refused are all schema rules, and the actor requirement is narrowed by condition rather than dropped, so every record that carries no sender still names the person. Every check-then-append passes through compare-and-append: nothing new appends, the resolution happens before decide, and test 14 forces a real interleaving through the gate's own policy-read seam and shows the loser is refused already-decided by re-derivation, with a variant at retryOnHeadMoved 1 proving the second write really does land between the first's read and its append. Refusals are machine-readable and distinct: sender-unmapped and sender-ambiguous are a new closed union of their own, CHANNEL_DECISION_REFUSAL_CODES, not two more members of the gate's, because decide cannot emit either; it is registered in SPEC section 11.2, pinned by tests/conformance.test.ts, and carried in conformance/vectors/refusal-unions.v1.json, which is now version 10.0.0.

One schema relaxation worth Carter's eye. audit.decision_refused required payload.actor. An unmapped sender is exactly the case where the runtime cannot name a person, and writing the listener's identity there would be the false record the event exists to prevent, so the requirement is now conditional: a record carrying no payload.sender must still name the human, and a record carrying one may omit it. Every refusal written before this rule still validates.

SPEC amendment called out to the human, criterion 4. Section 10.3 said the Telegram callback was verified against approver identity; it was verified against a configured chat id and the from object was never read. The sentence now states what is verified, a table states what each of the three channels can and cannot prove, and the mapping rule follows it. Section 5.2 gains the approvers senders bullet, section 6.3 the three audit fields, section 11.2 the new union's table.

Verification: npm run build, npm run typecheck and npm run lint clean; node conformance/run.mjs exits 0; targeted suites sender-identity, channels-telegram, channels-contract, channels-web, channels-cli, gate, concurrency, human-only, cli-gate, cli-doctor, cli-policy, cli-policy-apply, docs-guard, conformance, conformance-regen, event-schema, decision-refusal, fixtures, policy-load-route-floor, cli-help, cli-long-help ran 851 tests, 851 pass, 0 fail. The whole suite ran 4386 tests with 27 failures, all of them the known local email, TLS and packed-install cases this machine's Node fails regardless of this branch.

Status: staying In Progress. AC2 and AC4 are checked on this branch's evidence. AC3 was checked at design time for its documentation half and its other half is a live verification that is Carter's: step 6 of private/runbook-2026-09-17.md, one reject and one approve from the phone after the daemon restarts, then approval log verify, with the grant expected to name human:carter from the sender mapping and a second Telegram account expected to be refused. Issue 137 stays open until that runs. docs/proposals/sender-identity-2026-09.md is the mapping line for this repository's own policy, written in the contract docs/proposals/README.md defines; its Telegram id is a clearly marked placeholder, because no agent session knows Carter's account id and guessing one would refuse every tap on the phone until somebody edited the file from a terminal. The proposal says how to read the real id, including the cheapest way, which is to let one tap be refused and read the observed id off the record the refusal writes.
<!-- SECTION:NOTES:END -->
