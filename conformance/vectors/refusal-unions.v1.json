{
  "suite": "refusal-unions",
  "vectors_version": "22.0.0",
  "algorithm": "SPEC.md §11.1 invariant 6: refusals are machine-readable and distinct",
  "description": "The closed unions of refusal codes. A caller branches on these strings, so adding, removing, or renaming one is a breaking change and shows up here as a diff.",
  "provenance": "Generated by scripts/regen-conformance-vectors.mjs from this repository's own implementation. Inputs are authored by hand; every expectation is computed, never transcribed. See conformance/README.md for the runner contract.",
  "count": 13,
  "vectors": [
    {
      "id": "union-gate_refusal_codes",
      "description": "every way `approval register|request|decide|withdraw|expire` can refuse. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "gate_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "policy-not-attested",
          "policy-drift",
          "envelope-invalid",
          "task-file-unreadable",
          "task-already-registered",
          "envelope-missing",
          "not-registered",
          "action-not-registered",
          "duplicate-request",
          "already-executed",
          "budget-exceeded",
          "queue-full",
          "rate-limited",
          "payload-hash-required",
          "payload-mismatch",
          "payload-store-failed",
          "grant-classless-request",
          "class-human-only",
          "harness-launch-unruled",
          "loop-escalated",
          "not-delegated",
          "already-finished",
          "not-requested",
          "already-decided",
          "not-granted",
          "request-withdrawn",
          "not-requester",
          "expired",
          "not-expired",
          "actor-invalid",
          "actor-not-human",
          "actor-not-approver",
          "log-unreadable",
          "log-torn-tail",
          "log-corrupt",
          "diff-too-large",
          "proposal-not-found",
          "proposal-stale",
          "policy-already-attested",
          "reaction-note-required",
          "append-failed",
          "token-delivery-unavailable"
        ]
      }
    },
    {
      "id": "union-token_verify_refusal_codes",
      "description": "every way a presented token can fail verification. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "token_verify_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "not-granted",
          "token-mismatch",
          "token-consumed",
          "token-expired",
          "token-revoked",
          "payload-mismatch",
          "harness-executed"
        ]
      }
    },
    {
      "id": "union-token_refusal_codes",
      "description": "the token verbs' union: verification plus the log-and-append failures. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "token_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "not-granted",
          "token-mismatch",
          "token-consumed",
          "token-expired",
          "token-revoked",
          "payload-mismatch",
          "harness-executed",
          "class-human-only",
          "log-unreadable",
          "log-torn-tail",
          "log-corrupt",
          "append-failed"
        ]
      }
    },
    {
      "id": "union-execute_refusal_codes",
      "description": "every way `approval run` and the adapter contract can refuse. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "execute_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "actor-invalid",
          "action-not-registered",
          "class-human-only",
          "token-required",
          "loop-escalated",
          "policy-not-attested",
          "policy-drift",
          "already-executed",
          "budget-exceeded",
          "not-started",
          "already-finished",
          "not-granted",
          "token-mismatch",
          "token-consumed",
          "token-expired",
          "token-revoked",
          "harness-executed",
          "payload-mismatch",
          "actor-not-human",
          "execution-delegated",
          "execution-indeterminate",
          "not-indeterminate",
          "already-reconciled",
          "dangling-stdin-not-tty",
          "dangling-declined",
          "log-unreadable",
          "log-torn-tail",
          "log-corrupt",
          "append-failed"
        ]
      }
    },
    {
      "id": "union-append_error_codes",
      "description": "every way the write boundary itself can refuse an append. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "append_error_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "lock-timeout",
          "corrupt-tail",
          "validation",
          "canonicalization",
          "io",
          "head-moved",
          "daemon-id-invalid",
          "daemon-not-allowed"
        ]
      }
    },
    {
      "id": "union-anchor_refusal_codes",
      "description": "every way the log-anchoring check can refuse a working log that contradicts the committed copy of it. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "anchor_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "anchor-diverged"
        ]
      }
    },
    {
      "id": "union-checkpoint_refusal_codes",
      "description": "every way the human-signed checkpoint check can refuse a range whose signed heads the log contradicts. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "checkpoint_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "checkpoint-key-unknown",
          "checkpoint-signature-invalid",
          "checkpoint-hash-mismatch",
          "checkpoint-out-of-order",
          "checkpoint-malformed"
        ]
      }
    },
    {
      "id": "union-hook_deny_codes",
      "description": "every way `approval hook <harness>` can deny a tool call before it runs; `hook-gate-refused` is a family whose emitted form is `hook-gate-refused:<gate refusal code>`. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "hook_deny_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "hook-unclassified",
          "hook-class-human-only",
          "hook-harness-launch-unruled",
          "hook-opaque",
          "hook-unparseable",
          "hook-rejected",
          "hook-revoked",
          "hook-expired",
          "hook-withdrawn",
          "hook-timeout",
          "hook-harness-cap-too-short",
          "hook-gate-refused",
          "hook-grant-unverified",
          "hook-sandbox-required",
          "hook-policy-unavailable",
          "hook-log-unreachable",
          "hook-unsupported-execution-context",
          "hook-muse-contributor-model",
          "hook-hermes-execute-code-unbound",
          "hook-io"
        ]
      }
    },
    {
      "id": "union-post_tool_codes",
      "description": "every line the post-execution half of `approval hook <harness>` can print instead of closing a delegated execution; `post-tool-gate-refused` carries the gate's own code after a colon. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "post_tool_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "post-tool-reported",
          "post-tool-unidentified",
          "post-tool-not-gated",
          "post-tool-unreadable-outcome",
          "post-tool-log-unreachable",
          "post-tool-gate-refused",
          "post-tool-io"
        ]
      }
    },
    {
      "id": "union-channel_decision_refusal_codes",
      "description": "every way a decision SURFACE can refuse a human's gesture before the gate sees it: the sender the transport authenticated resolves to nobody, or to more than one person, in the attested policy, or the attested policy maps that channel's senders in the keyed form and the process holds no key to compute a digest with, or the gesture is an attestation whose in-force policy cannot say who is tapping. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "channel_decision_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "sender-unmapped",
          "sender-ambiguous",
          "sender-key-unavailable",
          "attest-requires-terminal"
        ]
      }
    },
    {
      "id": "union-bridge_refusal_codes",
      "description": "every way `approval codex bridge` can decline an app-server approval request on its own, before or instead of asking the gate: a request whose command, directory or call identity is missing, a command string that names no argv it can bind, an item-based file change whose content it cannot produce from the `item/started` frame that item id names, one whose item had already completed when the question arrived, and a server request it has no reading for. It is NOT the verb's whole vocabulary: `bridge_stop_codes` carries the ways it ends a session instead of answering a request. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "bridge_refusal_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "bridge-file-change-unbound",
          "bridge-unknown-request",
          "bridge-file-change-already-completed",
          "bridge-command-unbound",
          "bridge-request-unbound"
        ]
      }
    },
    {
      "id": "union-bridge_stop_codes",
      "description": "every way `approval codex bridge` STOPS a session rather than declining one request: a refused `thread/start`, a server reporting an effective approval policy that is not the pinned one, a preflight turn that ran no command so nothing was established, and a harness auto-reviewer notification saying something else answered a question before this client was asked. Separate from `bridge_refusal_codes` because the two boundaries differ: a decline answers one approval request and the turn carries on, a stop ends the run. A second implementation answers BOTH unions or has left a door open. Order is definition order; conformance means emitting exactly these codes, no more, no fewer.",
      "input": {
        "union": "bridge_stop_codes"
      },
      "expect": {
        "valid": true,
        "codes": [
          "bridge-thread-start-refused",
          "bridge-approval-policy-mismatch",
          "bridge-auto-reviewer-active",
          "bridge-preflight-void"
        ]
      }
    },
    {
      "id": "union-unknown",
      "description": "a union name no implementation defines must be refused, not answered with an empty list — an empty union would let a checker report full coverage of nothing",
      "control": true,
      "input": {
        "union": "there_is_no_such_union"
      },
      "expect": {
        "valid": false,
        "failure_class": "unknown-union"
      }
    }
  ]
}
